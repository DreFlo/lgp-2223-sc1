enum InstitutionType { education, work, other }

enum Priority { low, medium, high }

enum Periodicity { weekly, daily, monthly, yearly }

enum Status { nothing, planTo, goingThrough, done, dropped }

enum Emoji { happy, sad }
